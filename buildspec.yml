version: 0.2

phases:
  build:
    commands:
      - echo "Building AWS CodePipeline demo..."
      - echo "Build completed successfully!"
      - echo "THIS IS MY LATEST BUILD..."

artifacts:
  files:
    - index.html
